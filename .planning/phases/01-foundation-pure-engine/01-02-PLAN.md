---
phase: 01-foundation-pure-engine
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - package.json
  - postcss.config.mjs
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [FOUND-03, FOUND-04]
must_haves:
  truths:
    - "Tailwind v4 (or v3 documented fallback) installed and Blue Wave palette is available as utility classes"
    - "Press Start 2P loaded via next/font/google with display: 'swap' and variable: '--font-pixel'"
    - "globals.css imports tailwindcss and declares @theme block (or v3 equivalent)"
    - "Default Next welcome page replaced with bg-bg-dark + text-text-glow + font-pixel skeleton"
    - "If v4 lightningcss build fails, v3 fallback is installed and tailwind.config.ts has same Blue Wave colors"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @import + @theme block (or v3 @tailwind directives)"
      contains: "@theme|@tailwind"
    - path: "src/app/layout.tsx"
      provides: "RootLayout with Press_Start_2P from next/font/google bound to --font-pixel"
      contains: "Press_Start_2P"
    - path: "postcss.config.mjs"
      provides: "PostCSS config for v4 (@tailwindcss/postcss) or v3 (tailwindcss + autoprefixer)"
    - path: "package.json"
      provides: "Tailwind dependency pinned"
      contains: "tailwindcss"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Press Start 2P CSS variable"
      via: "Press_Start_2P({ variable: '--font-pixel', display: 'swap' }) on <html className={pressStart.variable}>"
      pattern: "Press_Start_2P|--font-pixel"
    - from: "src/app/globals.css"
      to: "Blue Wave palette"
      via: "@theme block exports --color-electric: #00BFFF etc. (v4) or tailwind.config.ts colors (v3)"
      pattern: "00BFFF"
    - from: "src/app/layout.tsx"
      to: "globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
---

<objective>
Install Tailwind v4 (with documented v3 fallback) and configure Blue Wave palette via @theme. Load Press Start 2P via next/font/google bound to `--font-pixel` CSS variable with `display: 'swap'` per FOUND-04. Replace the create-next-app default welcome page with a minimal Blue Wave skeleton page so the next person to run `npm run dev` sees the palette and font correctly applied.

Purpose: Lock the visual ground rules (Blue Wave palette as Tailwind utilities, pixel font as default) so every later phase composes against a stable theme. This is the only place in Phase 1 that touches CSS / fonts / palette.

Output: globals.css with Blue Wave @theme, layout.tsx with Press Start 2P, page.tsx skeleton showing the palette + font in action. Dev server visibly renders Blue Wave dark background with pixel font.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-pure-engine/01-RESEARCH.md
@.planning/PROJECT.md
@.planning/UI-ASSETS.md
@.planning/phases/01-foundation-pure-engine/01-01-SUMMARY.md

<interfaces>
<!-- Blue Wave palette (RESEARCH §4, PROJECT.md, UI-ASSETS.md §1) -->
--color-electric:    #00BFFF   /* primary action / focus */
--color-cobalt:      #0047AB   /* depth / borders */
--color-cyan-neon:   #00FFFF   /* highlights / OVERDRIVE accents (later) */
--color-shadow-cold: #0A0A1A   /* panel backgrounds */
--color-bg-dark:     #050510   /* page background */
--color-text-glow:   #7DF9FF   /* primary text color */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt Tailwind v4 upgrade with v3 fallback path</name>
  <files>package.json, postcss.config.mjs, src/app/globals.css, tailwind.config.ts (only if v3 fallback)</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (sections 1.2, 1.3, 1.4, 4 — install, postcss config, v3 fallback, @theme block)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/package.json
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/postcss.config.mjs (the one create-next-app generated)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/src/app/globals.css (the default v3 file from scaffold)
  </read_first>
  <action>
    Per RESEARCH §1.2: attempt Tailwind v4 first, fall back to v3 if `lightningcss` fails. Both paths use the SAME Blue Wave color names so the rest of Phase 1 is invariant to which version landed.

    STEP 1 — Attempt v4:

    From project root `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG`:

    ```bash
    npm uninstall tailwindcss postcss autoprefixer
    npm install -D tailwindcss@^4 @tailwindcss/postcss@^4
    ```

    STEP 2 — Write v4 PostCSS config:

    Replace `postcss.config.mjs` with EXACTLY:

    ```javascript
    export default {
      plugins: {
        '@tailwindcss/postcss': {},
      },
    };
    ```

    STEP 3 — Write v4 globals.css with @theme block:

    Replace `src/app/globals.css` with EXACTLY:

    ```css
    @import "tailwindcss";

    @theme {
      /* Blue Wave palette — from PROJECT.md §"Paleta Blue Wave" / RESEARCH §4 */
      --color-electric:    #00BFFF;
      --color-cobalt:      #0047AB;
      --color-cyan-neon:   #00FFFF;
      --color-shadow-cold: #0A0A1A;
      --color-bg-dark:     #050510;
      --color-text-glow:   #7DF9FF;

      /* Pixel font — bound to next/font CSS variable from layout.tsx (Task 2) */
      --font-pixel: var(--font-pixel);
    }

    :root {
      color-scheme: dark;
      background-color: var(--color-bg-dark);
      color: var(--color-text-glow);
      font-family: var(--font-pixel), monospace;
    }

    html, body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
    }

    img {
      image-rendering: pixelated;
    }
    ```

    STEP 4 — Test v4 build:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npm run build 2>&1 | tee /tmp/v4-build.log
    ```

    INSPECT THE LOG. If you see `lightningcss` errors (e.g., "Cannot find module '../lightningcss.linux-x64-gnu.node'", "Failed to load native binding", or any reference to lightningcss failing) → proceed to STEP 5 (v3 fallback).

    If build succeeds → SKIP STEP 5 and STEP 6, proceed to verification.

    STEP 5 — v3 FALLBACK (only if v4 lightningcss failed):

    ```bash
    npm uninstall tailwindcss @tailwindcss/postcss
    npm install -D tailwindcss@^3 postcss@^8 autoprefixer@^10
    ```

    Replace `postcss.config.mjs` with v3 plugin format:

    ```javascript
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    };
    ```

    Create `tailwind.config.ts` with EXACTLY:

    ```typescript
    import type { Config } from 'tailwindcss';

    const config: Config = {
      content: ['./src/**/*.{ts,tsx}'],
      theme: {
        extend: {
          colors: {
            electric:     '#00BFFF',
            cobalt:       '#0047AB',
            'cyan-neon':  '#00FFFF',
            'shadow-cold':'#0A0A1A',
            'bg-dark':    '#050510',
            'text-glow':  '#7DF9FF',
          },
          fontFamily: {
            pixel: ['var(--font-pixel)', 'monospace'],
          },
        },
      },
      plugins: [],
    };

    export default config;
    ```

    Replace `src/app/globals.css` with v3 directives:

    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    :root {
      color-scheme: dark;
      background-color: #050510;
      color: #7DF9FF;
      font-family: var(--font-pixel), monospace;
    }

    html, body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
    }

    img {
      image-rendering: pixelated;
    }
    ```

    Re-test build:

    ```bash
    npm run build
    ```

    STEP 6 — Document path taken: in your Task summary, note "Tailwind v4 succeeded" OR "Tailwind v3 fallback used due to lightningcss error: <error excerpt>".

    Either way: utility class names (`bg-bg-dark`, `text-text-glow`, `font-pixel`, `bg-electric`, etc.) work identically across v3 and v4 paths.
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && { grep -q "@theme" src/app/globals.css || grep -q "@tailwind base" src/app/globals.css; } && { grep -q "00BFFF" src/app/globals.css || grep -q "00BFFF" tailwind.config.ts; } && { grep -q "@tailwindcss/postcss" postcss.config.mjs || grep -q "tailwindcss:" postcss.config.mjs; } && grep -q "tailwindcss" package.json && npm run build 2>&1 | grep -qE "(Compiled successfully|✓ Compiled)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `tailwindcss` in devDependencies (either ^4 or ^3)
    - `postcss.config.mjs` references either `@tailwindcss/postcss` (v4) OR `tailwindcss` + `autoprefixer` (v3)
    - `src/app/globals.css` either contains `@theme` block (v4) with `--color-electric: #00BFFF` OR contains `@tailwind base/components/utilities` directives (v3)
    - If v3 fallback: `tailwind.config.ts` exists with `electric: '#00BFFF'` and `pixel: ['var(--font-pixel)', 'monospace']`
    - `npm run build` exits 0
    - Blue Wave palette `#00BFFF` appears either in `globals.css` (v4) or `tailwind.config.ts` (v3)
  </acceptance_criteria>
  <done>Tailwind installed (v4 or v3 fallback), Blue Wave palette wired, globals.css configured, npm run build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Press Start 2P via next/font/google in layout.tsx + skeleton page</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (section 5 — next/font snippet, exact import format)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/src/app/layout.tsx (the one create-next-app generated)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/src/app/page.tsx (the one create-next-app generated)
  </read_first>
  <action>
    PART A — Replace `src/app/layout.tsx` with EXACTLY (per RESEARCH §5):

    ```typescript
    import type { Metadata } from 'next';
    import { Press_Start_2P } from 'next/font/google';
    import './globals.css';

    const pressStart = Press_Start_2P({
      weight: '400',                  // Press Start 2P only ships in 400
      subsets: ['latin'],
      display: 'swap',                // FOUND-04 requirement (RESEARCH A8: swap wins over PITFALLS block recommendation)
      variable: '--font-pixel',       // Exposes CSS var consumed by @theme + :root in globals.css
      preload: true,
    });

    export const metadata: Metadata = {
      title: '[In]terfaces — Demo',
      description: 'Arcologia Casting-7. 4 encontros. Sobreviva.',
    };

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="pt-BR" className={pressStart.variable}>
          <body>{children}</body>
        </html>
      );
    }
    ```

    Notes:
    - `weight: '400'` — Press Start 2P is single-weight; literal type required.
    - `variable: '--font-pixel'` — produces a CSS variable on `<html>` that globals.css `--font-pixel: var(--font-pixel)` references.
    - `display: 'swap'` — REQUIRED by FOUND-04 (overrides PITFALLS.md preference for `'block'`; document FOUT acceptance for Phase 5 review).

    PART B — Replace `src/app/page.tsx` with a minimal Blue Wave skeleton so the dev server visibly proves palette + font are applied (Plan 07 will replace this with `<BattleScene />`):

    ```typescript
    export default function Page() {
      return (
        <main className="min-h-screen bg-bg-dark text-text-glow font-pixel p-8">
          <h1 className="text-electric text-2xl mb-4">[In]terfaces — Foundation Skeleton</h1>
          <p className="mb-2">
            Tailwind <span className="text-cyan-neon">Blue Wave</span> palette active.
          </p>
          <p className="mb-2">
            Press Start 2P loaded via <span className="text-electric">next/font/google</span> with
            <code className="bg-shadow-cold px-2 py-1 ml-1">display: &apos;swap&apos;</code>.
          </p>
          <p className="opacity-60 text-xs mt-8">
            Phase 1 — Plan 02 — visual ground truth. Replaced by &lt;BattleScene /&gt; in Plan 07.
          </p>
        </main>
      );
    }
    ```

    PART C — Verify build:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npx tsc --noEmit
    npm run build
    ```

    Both must exit 0.
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && grep -q "Press_Start_2P" src/app/layout.tsx && grep -q "from 'next/font/google'" src/app/layout.tsx && grep -q "variable: '--font-pixel'" src/app/layout.tsx && grep -q "display: 'swap'" src/app/layout.tsx && grep -q "weight: '400'" src/app/layout.tsx && grep -q "pressStart.variable" src/app/layout.tsx && grep -q "bg-bg-dark" src/app/page.tsx && grep -q "font-pixel" src/app/page.tsx && npx tsc --noEmit && npm run build 2>&1 | grep -qE "(Compiled successfully|✓ Compiled)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` imports `Press_Start_2P` from `'next/font/google'`
    - `src/app/layout.tsx` declares font with `weight: '400'`, `subsets: ['latin']`, `display: 'swap'`, `variable: '--font-pixel'`
    - `<html>` element has `className={pressStart.variable}` applied
    - `src/app/page.tsx` uses Tailwind classes `bg-bg-dark`, `text-text-glow`, `font-pixel`, `text-electric`, `text-cyan-neon` (proves palette utilities resolve in either v4 or v3 path)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Press Start 2P bound to --font-pixel via next/font/google with swap display, page.tsx skeleton renders Blue Wave palette + pixel font. tsc and build both clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN → next/font build pipeline | next/font fetches Press Start 2P at BUILD time and self-hosts; runtime has zero external font requests |
| lightningcss native binding → npm install | Tailwind v4 uses native lightningcss; platform-specific binary may fail on some envs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | D (Denial of Service) | Tailwind v4 lightningcss build failure on user's machine | mitigate | v3 fallback fully documented in RESEARCH §1.4 and STEP 5 above; same palette utility classes work in both versions |
| T-02-02 | I (Information Disclosure) | Runtime font fetches leaking IP to Google | mitigate | next/font/google self-hosts at build time per Next.js docs; zero runtime requests to Google domains (FOUND-04 chose this path explicitly) |
| T-02-03 | T (Tampering) | FOUT (flash of unstyled text) showing fallback monospace before pixel font loads on slow connections | accept | FOUND-04 mandates `display: 'swap'` over PITFALLS.md `'block'` recommendation; documented for Phase 5 polish review (RESEARCH A8) |
</threat_model>

<verification>
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `grep -q "Press_Start_2P" src/app/layout.tsx` succeeds
- `grep -q "00BFFF" src/app/globals.css` OR `grep -q "00BFFF" tailwind.config.ts` succeeds
- Either Tailwind v4 (`@theme` in globals.css) OR v3 (`@tailwind` directives + `tailwind.config.ts`) is present, not both
</verification>

<success_criteria>
- FOUND-03 SATISFIED: Tailwind (v4 or v3 fallback) configured with Blue Wave palette accessible as utility classes
- FOUND-04 SATISFIED: Press Start 2P loaded via next/font/google with display: 'swap' and variable: '--font-pixel'
- Visual smoke (Task 4 of Plan 01 already verified dev boots; running `npm run dev` after this plan would show Blue Wave dark bg + pixel font)
- Plan 03 (Vitest) can run in parallel — files modified do not overlap
- Plan 04 onward can use `bg-electric`, `text-text-glow`, `font-pixel` utility classes confidently
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-engine/01-02-SUMMARY.md` documenting:
- Final Tailwind version installed (v4 or v3 fallback)
- If v3 fallback: the exact lightningcss error excerpt that triggered it
- Confirmation that Press Start 2P loads via next/font/google self-hosted (no runtime external requests)
- Note: FOUT risk per `display: 'swap'` is documented for Phase 5 polish review
</output>
