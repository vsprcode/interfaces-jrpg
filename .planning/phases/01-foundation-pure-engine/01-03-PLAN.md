---
phase: 01-foundation-pure-engine
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - package.json
  - vitest.config.ts
  - src/engine/_smoke.test.ts
autonomous: true
requirements: [FOUND-06]
must_haves:
  truths:
    - "Vitest 2.x is installed as a dev dependency"
    - "vitest.config.ts is configured with environment: 'node' (no DOM in Phase 1)"
    - "package.json has scripts: test, test:watch, test:ui, test:coverage"
    - "npm run test exits 0 with at least one passing smoke test"
    - "Coverage thresholds (80% lines/branches/functions/statements) wired for src/engine/**"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with node environment and engine coverage scope"
      contains: "environment: 'node'"
    - path: "src/engine/_smoke.test.ts"
      provides: "1+1=2 smoke test confirming Vitest runs (deleted in Plan 04 once damage.test.ts replaces it)"
      contains: "expect"
    - path: "package.json"
      provides: "test scripts + vitest devDependency"
      contains: "vitest"
  key_links:
    - from: "vitest.config.ts"
      to: "src/engine/**/*.test.ts"
      via: "include glob"
      pattern: "src/engine/\\*\\*"
    - from: "package.json scripts.test"
      to: "vitest run"
      via: "npm run test → vitest run"
      pattern: "vitest"
---

<objective>
Install Vitest 2 and configure it for pure-TypeScript engine tests (no DOM, no jsdom — Phase 1 has no component tests). Add npm scripts for one-shot run, watch mode, UI dashboard, and coverage. Drop a 1+1=2 smoke test under `src/engine/` to prove the config works end-to-end. The smoke test gets deleted in Plan 04 when `damage.test.ts` lands.

Purpose: Establish the "Wave 0" test infrastructure required by every subsequent Plan 04+ task. No engine code can be written without a green test runner; this plan exists to make `npm run test -- --run` an instant feedback loop (< 5s) for all later TDD work in Phase 1.

Output: Working `npm run test` that finds and runs `src/engine/**/*.test.ts` files in node environment. Coverage scaffolding ready for v8 reporter. Plan 04 onward can write `*.test.ts` files alongside production modules with zero additional setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-pure-engine/01-RESEARCH.md
@.planning/phases/01-foundation-pure-engine/01-VALIDATION.md
@.planning/phases/01-foundation-pure-engine/01-01-SUMMARY.md

<!-- Independent of Plan 02 (Style + Font). Both can run parallel in Wave 1. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest 2 and add npm scripts</name>
  <files>package.json</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (sections 1.5, 6 — install commands and vitest config)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/package.json
  </read_first>
  <action>
    From project root:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npm install -D vitest@^2 @vitest/ui@^2 @types/node@^20
    ```

    Then read current `package.json` and merge these scripts into the `scripts` block (preserve `dev`, `build`, `start`, `lint` from Plan 01):

    ```json
    {
      "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "test:ui": "vitest --ui",
        "test:coverage": "vitest run --coverage"
      }
    }
    ```

    Notes:
    - `vitest run` is one-shot (CI-friendly, no watch). VALIDATION.md sampling commands use `--run` flag — `vitest run` is shorthand for the same.
    - `@vitest/ui` is optional but useful for `npm run test:ui` browser dashboard during development.
    - `@types/node` is REQUIRED for `vitest.config.ts` to import `defineConfig` cleanly under our strict tsconfig.

    Do NOT install `@testing-library/react`, `@testing-library/user-event`, `jsdom`, or `@vitest/coverage-v8` separately — Phase 1 uses node environment, not DOM. Coverage's v8 provider is built into Node 18+ (no extra package). DOM testing tools land in Phase 2.
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && grep -q '"vitest":' package.json && grep -q '"test": "vitest run"' package.json && grep -q '"test:coverage"' package.json && test -d node_modules/vitest</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains `vitest` (^2.x), `@vitest/ui` (^2.x), `@types/node` (^20.x)
    - `package.json` scripts contains `test`, `test:watch`, `test:ui`, `test:coverage`
    - `node_modules/vitest/` directory exists
    - `npm install` completed with exit code 0
    - Existing `dev`, `build`, `start`, `lint` scripts from Plan 01 are still present
  </acceptance_criteria>
  <done>Vitest 2 installed, npm scripts wired, no DOM testing tools added (deferred to Phase 2).</done>
</task>

<task type="auto">
  <name>Task 2: Write vitest.config.ts (node environment, engine coverage scope)</name>
  <files>vitest.config.ts, src/engine/_smoke.test.ts</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (section 6 — vitest config exact content + smoke test)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/tsconfig.json (strict flags affect config file too)
  </read_first>
  <action>
    PART A — Create `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/vitest.config.ts` with EXACTLY (per RESEARCH §6):

    ```typescript
    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        environment: 'node',          // Phase 1: pure TS engine, no DOM (FOUND-06)
        globals: true,                // Allow describe/it/expect without imports
        include: ['src/engine/**/*.test.ts'],
        coverage: {
          provider: 'v8',             // built into Node 18+
          reporter: ['text', 'html'],
          include: ['src/engine/**/*.ts'],
          exclude: ['src/engine/**/*.test.ts', 'src/engine/types.ts'],
          thresholds: {
            lines: 80,
            functions: 80,
            branches: 80,
            statements: 80,
          },
        },
      },
    });
    ```

    Notes:
    - `environment: 'node'` because Phase 1 has zero DOM tests. Plan 06 BattleScene tests stay under `src/components/` and will get their own jsdom config in Phase 2 (per RESEARCH §6 future-extension note).
    - `globals: true` lets us write `describe('foo', () => ...)` without explicit imports — matches RESEARCH code samples in §8.2, §9.3, §10.1, §12.1.
    - `include: ['src/engine/**/*.test.ts']` — limits the runner to engine tests this phase. If Plan 06 wants smoke tests under `src/components/`, that's a follow-up config change.
    - `coverage.exclude: ['src/engine/types.ts']` — types.ts is pure type declarations and produces no runtime code to cover.

    PART B — Create the smoke test at `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/src/engine/_smoke.test.ts` (RESEARCH §6):

    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('vitest smoke', () => {
      it('runs', () => {
        expect(1 + 1).toBe(2);
      });
    });
    ```

    Note: leading underscore on `_smoke.test.ts` — Plan 04 deletes this file once `damage.test.ts` lands. Until then it's the only test that runs.

    PART C — Run the test suite:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npm run test
    ```

    Expected output: 1 test file, 1 test passed, exit code 0, runtime < 5 seconds (cold), < 2 seconds (warm).

    Also verify `tsc --noEmit` still passes — vitest.config.ts is a TS file under our strict config:

    ```bash
    npx tsc --noEmit
    ```

    Should exit 0. If `vitest/config` triggers a strict-mode error (e.g., `defineConfig` overload mismatch), check that `@types/node` is installed and that `vitest` was installed under devDependencies (not dependencies).
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && test -f vitest.config.ts && grep -q "environment: 'node'" vitest.config.ts && grep -q "src/engine/\*\*/\*.test.ts" vitest.config.ts && grep -q "thresholds:" vitest.config.ts && test -f src/engine/_smoke.test.ts && npm run test 2>&1 | grep -qE "(1 passed|✓.*runs)" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at project root
    - `vitest.config.ts` declares `environment: 'node'`
    - `vitest.config.ts` declares `include: ['src/engine/**/*.test.ts']`
    - `vitest.config.ts` declares `coverage.thresholds` with lines/functions/branches/statements all 80
    - `vitest.config.ts` declares `coverage.include: ['src/engine/**/*.ts']` and excludes `*.test.ts` and `types.ts`
    - `src/engine/_smoke.test.ts` exists with `expect(1 + 1).toBe(2)` test
    - `npm run test` exits 0 with output indicating 1 test passed
    - `npx tsc --noEmit` exits 0 (vitest.config.ts compiles under strict tsconfig)
  </acceptance_criteria>
  <done>Vitest configured for node environment with engine-scoped coverage, smoke test passes, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → production code | Vitest in node environment imports src/engine/ TS modules directly via Vite transformer; no SSR or DOM coupling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | Test runner accidentally executing browser-only code (window, document, etc.) | mitigate | `environment: 'node'` enforced in config; `include` glob limits scope to `src/engine/**`; Plan 04+ engine code is pure TS by design |
| T-03-02 | D (Denial of Service) | Vitest hung in watch mode in CI/scripts | mitigate | `npm run test` uses `vitest run` (one-shot); VALIDATION.md sampling commands use `--run` flag explicitly |
| T-03-03 | I (Information Disclosure) | Coverage HTML reports committed to repo | accept | `.gitignore` from Plan 01 includes `coverage/` so HTML output stays local |
</threat_model>

<verification>
- `test -f vitest.config.ts` succeeds
- `npm run test` exits 0
- `npx tsc --noEmit` exits 0
- `grep -q "environment: 'node'" vitest.config.ts` succeeds
- Test runtime under 5 seconds cold (per VALIDATION.md sampling target)
</verification>

<success_criteria>
- FOUND-06 SATISFIED: Vitest 2 configured for engine tests in pure TS / node environment
- Wave 0 test infrastructure complete (per VALIDATION.md "Wave 0 Requirements" — vitest.config.ts and test scripts ready)
- `npm run test -- --run` provides < 5s feedback loop for all Plan 04+ TDD work
- No DOM testing tools installed (correctly deferred to Phase 2)
- Plan 04 (Engine Types & Pure Functions) can begin in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-engine/01-03-SUMMARY.md` documenting:
- Final vitest version installed (`npm list vitest --depth=0`)
- Smoke test runtime (cold and warm)
- Confirmation that DOM testing tools were NOT installed
- Note: `_smoke.test.ts` deleted in Plan 04 once `damage.test.ts` exists
</output>
