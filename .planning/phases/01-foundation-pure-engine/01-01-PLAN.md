---
phase: 01-foundation-pure-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: false
requirements: [FOUND-01, FOUND-05, FOUND-07, FOUND-08]
must_haves:
  truths:
    - "git is initialized in project root with Next.js .gitignore covering node_modules, .next, coverage"
    - "npm run dev boots Next.js 14 on localhost:3000 with React Strict Mode enabled"
    - "Folder structure exists: src/app, src/components, src/engine, src/data, src/styles"
    - "TypeScript strict mode (with extended flags) compiles cleanly via npx tsc --noEmit"
    - "create-next-app scaffolding does not overwrite .planning/, CLAUDE.md, or this PLAN file"
  artifacts:
    - path: ".gitignore"
      provides: "Next.js ignore rules"
      contains: "node_modules"
    - path: "package.json"
      provides: "Next.js 14 + TypeScript 5 dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration with extra strict flags"
      contains: "strict"
    - path: "next.config.ts"
      provides: "Next config with reactStrictMode: true"
      contains: "reactStrictMode"
    - path: "src/app/layout.tsx"
      provides: "App Router root layout"
    - path: "src/app/page.tsx"
      provides: "App Router home page"
    - path: "src/components"
      provides: "Components directory (FOUND-05)"
    - path: "src/engine"
      provides: "Engine directory (FOUND-05)"
    - path: "src/data"
      provides: "Data directory (FOUND-05)"
    - path: "src/styles"
      provides: "Styles directory (FOUND-05)"
  key_links:
    - from: ".gitignore"
      to: "Next.js artifacts"
      via: "ignore patterns"
      pattern: "node_modules|\\.next"
    - from: "next.config.ts"
      to: "React Strict Mode"
      via: "reactStrictMode: true"
      pattern: "reactStrictMode:\\s*true"
    - from: "tsconfig.json"
      to: "TypeScript strict mode"
      via: "strict: true + noUncheckedIndexedAccess + noFallthroughCasesInSwitch + exactOptionalPropertyTypes"
      pattern: "noUncheckedIndexedAccess"
---

<objective>
Scaffold the Next.js 14 + TypeScript strict project foundation: initialize git with proper .gitignore, run `create-next-app@14` into the existing project directory (preserving .planning/ and CLAUDE.md), upgrade tsconfig to full strict family, enable React Strict Mode in next.config.ts, and create the FOUND-05 folder structure. Verify dev server boots on localhost:3000 and `tsc --noEmit` is clean.

Purpose: Lock the structural ground rules (Next 14 App Router, TypeScript strict, src-dir layout, Strict Mode) before any engine code is written. Every Phase 1 pitfall guardrail depends on Strict Mode being on and TypeScript strict catching the right bug classes.

Output: Working Next.js scaffold ready for Tailwind v4 + Press Start 2P (Plan 02) and Vitest (Plan 03) to layer on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-pure-engine/01-RESEARCH.md
@.planning/phases/01-foundation-pure-engine/01-VALIDATION.md

<!-- Note: CLAUDE.md "Recommended Stack" lists Zustand. This is SUPERSEDED by SUMMARY.md
     "Stack Conflict Resolution" — Phase 1 uses useReducer only. Do NOT install Zustand. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git and verify environment</name>
  <files>.gitignore</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (sections 1.5, 1.6, 17 — install commands and verification)
  </read_first>
  <action>
    Verify environment prereqs and initialize git BEFORE running create-next-app (so the scaffold does not refuse to initialize a new git repo over an existing one).

    Steps (use absolute paths in all commands; treat the project root as `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG`):

    1. Verify Node and npm versions:
       ```bash
       node --version    # MUST be >= v20.18 (Tailwind v4 lightningcss requirement per RESEARCH §1, A1)
       npm --version
       git --version
       ```
       If Node < 20.18, STOP and instruct user to install Node 20 LTS or 22 LTS.

    2. Initialize git in project root if not already:
       ```bash
       cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
       test -d .git || git init
       ```

    3. Verify or create initial `.gitignore` covering Next.js artifacts (create-next-app will overwrite this in Task 2 — that's fine):
       ```
       node_modules/
       .next/
       out/
       coverage/
       *.log
       .env*.local
       .DS_Store
       ```

    4. Confirm `.planning/` directory and CLAUDE.md exist and will be preserved (do not delete; create-next-app `.` keeps them):
       ```bash
       test -d ".planning" && test -f "CLAUDE.md" && echo "OK: planning artifacts present"
       ```

    Do NOT run `create-next-app` yet — Task 2 handles that and requires the directory to be set up.
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && test -d .git && test -f .gitignore && grep -q "node_modules" .gitignore && grep -q "\.next" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .git` succeeds (git initialized)
    - `test -f .gitignore` succeeds
    - `grep -q "node_modules" .gitignore` succeeds
    - `grep -q "\.next" .gitignore` succeeds
    - `node --version` reports v20.18 or higher
    - `.planning/` directory and `CLAUDE.md` are unchanged (still present)
  </acceptance_criteria>
  <done>Git initialized, .gitignore covers Next.js artifacts, environment prereqs verified, .planning/ and CLAUDE.md untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT): User runs create-next-app interactively</name>
  <action>
    This is a human checkpoint task. The actionable work is described in <how-to-verify> below — the human user performs those steps directly. Claude pauses execution at this checkpoint until the user types the resume signal. No automated work to perform.
  </action>
  <what-built>Task 1 verified env and git. The scaffold step requires running `npx create-next-app@14 .` in INTERACTIVE mode because:
    1. The CLI prompts "directory not empty, continue?" and the user must accept (typing "y" via human input keeps the safety check honest)
    2. create-next-app may pull network resources and prompt for tracking opt-in
    3. The scaffold writes 50+ files; failure mid-scaffold leaves the directory in a half-state that an automated retry would worsen
  </what-built>
  <how-to-verify>
    Run THIS EXACT command from the project root (your terminal — NOT through Claude):

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npx create-next-app@14 . \
      --typescript \
      --app \
      --tailwind \
      --src-dir \
      --eslint \
      --no-import-alias
    ```

    When prompted "directory contains files, continue?" → answer YES.
    When prompted about telemetry / Turbopack / etc. → accept defaults (the flags above lock the important choices).

    After scaffold completes:
    1. Confirm `package.json`, `next.config.mjs` (or `.ts`), `tsconfig.json`, `src/app/page.tsx`, `src/app/layout.tsx`, `src/app/globals.css` all exist
    2. Confirm `.planning/` and `CLAUDE.md` are STILL present (scaffold should not have deleted them)
    3. Run `npm run dev` — should boot to `localhost:3000` showing the default Next.js welcome page (Tailwind v3 default theme, no Blue Wave yet — that's Plan 02)
    4. Stop the dev server (Ctrl+C)

    Report back: "scaffold succeeded" and any deviations from the file list above.
  </how-to-verify>
  <resume-signal>Type "scaffold complete" once npm run dev boots cleanly and .planning/ + CLAUDE.md are preserved. If scaffold failed or overwrote planning artifacts, type "FAIL: <description>" and stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tighten tsconfig + enable Strict Mode + create FOUND-05 folders</name>
  <files>tsconfig.json, next.config.ts, src/components/.gitkeep, src/engine/.gitkeep, src/data/.gitkeep, src/styles/.gitkeep</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (sections 2, 3, 16 — next.config.ts content, tsconfig delta, directory layout)
    - tsconfig.json (the one create-next-app just generated — read before editing)
    - next.config.mjs OR next.config.ts (the one create-next-app generated)
  </read_first>
  <action>
    PART A — tsconfig.json delta (replace contents with the strict-family configuration):

    Overwrite `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/tsconfig.json` with EXACTLY:

    ```jsonc
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": false,
        "skipLibCheck": true,
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "noImplicitOverride": true,
        "noFallthroughCasesInSwitch": true,
        "exactOptionalPropertyTypes": true,
        "forceConsistentCasingInFileNames": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "noEmit": true,
        "plugins": [{ "name": "next" }],
        "paths": {
          "@/*": ["./src/*"]
        }
      },
      "include": ["next-env.d.ts", "src/**/*.ts", "src/**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    Note: We override `--no-import-alias` flag from create-next-app and ADD `paths` for `@/*` imports — research §3 recommends this for engine imports later. If create-next-app's default tsconfig already has `paths` configured, ensure ours wins.

    PART B — next.config.ts (rename if needed, write content):

    1. If `next.config.mjs` exists, delete it (we use `.ts` for type safety per RESEARCH §2):
       ```bash
       rm -f "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/next.config.mjs"
       ```

    2. Write `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/next.config.ts` with EXACTLY:

       ```typescript
       // next.config.ts
       import type { NextConfig } from 'next';

       const nextConfig: NextConfig = {
         reactStrictMode: true,         // CRITICAL: Pitfall 1 must be caught in dev (RESEARCH §15.1)
       };

       export default nextConfig;
       ```

    PART C — Create FOUND-05 folders:

    create-next-app already created `src/app/`. Create the remaining four folders with `.gitkeep` placeholder files so git tracks them:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    mkdir -p src/components src/engine src/data src/styles
    touch src/components/.gitkeep src/engine/.gitkeep src/data/.gitkeep src/styles/.gitkeep
    ```

    PART D — Update package.json scripts (preserve existing test-related scripts that may be added in later plans). Read current `package.json` first, then ensure these scripts are present (merge, don't overwrite other entries):

    ```json
    {
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "lint": "next lint"
      }
    }
    ```

    Do NOT add `--turbo` to dev script in Phase 1 — Turbopack is opt-in via flag at run time and the research §2 marks it as optional. Keep dev script standard.

    PART E — Verify:

    ```bash
    cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
    npx tsc --noEmit
    ```

    Expected: zero errors. If there are errors from create-next-app's default `src/app/page.tsx` (e.g., due to `exactOptionalPropertyTypes`), inspect them — most likely the default `<Image>` props or example code triggers the new strict flags. If so:
    - For Phase 1, MINIMAL fix the errors in `src/app/page.tsx` and `src/app/layout.tsx` directly (Plan 02 will rewrite both anyway)
    - Document any non-trivial fix in the task summary
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && grep -q '"strict": true' tsconfig.json && grep -q '"noUncheckedIndexedAccess": true' tsconfig.json && grep -q '"exactOptionalPropertyTypes": true' tsconfig.json && grep -q '"noFallthroughCasesInSwitch": true' tsconfig.json && test -f next.config.ts && grep -q "reactStrictMode: true" next.config.ts && test -d src/components && test -d src/engine && test -d src/data && test -d src/styles && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` contains all four extra strict flags (`noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `exactOptionalPropertyTypes`)
    - `tsconfig.json` contains `"strict": true`
    - `tsconfig.json` contains `"paths": { "@/*": ["./src/*"] }`
    - `next.config.ts` exists (not `.mjs`) and contains `reactStrictMode: true`
    - `next.config.mjs` does NOT exist (was deleted)
    - All four FOUND-05 folders exist: `src/components/`, `src/engine/`, `src/data/`, `src/styles/` (each tracked by git via `.gitkeep`)
    - `npx tsc --noEmit` exits 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>tsconfig is strict-family, next.config.ts has Strict Mode on, FOUND-05 folders exist, tsc --noEmit is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): Verify dev server boots on :3000 with Strict Mode active</name>
  <action>
    This is a human checkpoint task. The actionable work is described in <how-to-verify> below — the human user performs those steps directly. Claude pauses execution at this checkpoint until the user types the resume signal. No automated work to perform.
  </action>
  <what-built>Tasks 1-3 produced a Next.js 14 scaffold with TypeScript strict family enabled, React Strict Mode on, and all FOUND-05 directories present. We need a human to confirm `npm run dev` actually boots and that React Strict Mode is firing in the browser console.</what-built>
  <how-to-verify>
    From the project root:

    1. Run `npm run dev`
    2. Confirm output includes "Ready in XXXms" within 5 seconds (FOUND-01 success criterion)
    3. Open `http://localhost:3000` in browser
    4. Confirm the default Next.js welcome page renders (Tailwind v3 styling — Blue Wave palette comes in Plan 02)
    5. Open DevTools → Console
    6. Confirm NO red errors (warnings about Strict Mode double-mounting are EXPECTED and good — they confirm reactStrictMode: true is active)
    7. Stop the dev server (Ctrl+C)

    Then verify production build:
    8. Run `npm run build`
    9. Confirm build completes successfully (FOUND-08 dry run)
    10. Verify TypeScript exits clean: `npx tsc --noEmit`

    Report any deviations.
  </how-to-verify>
  <resume-signal>Type "approved" if dev server boots, page renders, no console errors, and `npm run build` succeeds. Type "FAIL: <description>" otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → project | create-next-app writes 50+ files; risk of overwriting `.planning/` or CLAUDE.md |
| npm registry → local | npm install pulls third-party code (Next.js + transitive deps) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | create-next-app overwriting planning artifacts | mitigate | Task 1 verifies `.planning/` and `CLAUDE.md` exist before scaffold; Task 2 checkpoint requires user to confirm they survive |
| T-01-02 | I (Information Disclosure) | npm dep supply chain (npm install pulls thousands of packages) | accept | Standard Next.js scaffold; lockfile committed in subsequent commits; no secrets in repo |
| T-01-03 | D (Denial of Service) | Strict Mode disabled hides Pitfall 1 bugs (battle timer double-fire) | mitigate | next.config.ts pins `reactStrictMode: true`; Plan 06 verifies no double-fire in BattleScene |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run dev` boots within 5s on :3000 with no console errors
- `npm run build` exits 0
- `.planning/` directory and `CLAUDE.md` are intact (untouched by scaffold)
- All FOUND-05 directories present
- `next.config.ts` has `reactStrictMode: true`
- `tsconfig.json` has the four extra strict flags
</verification>

<success_criteria>
- FOUND-01 SATISFIED: Next.js 14 boots on localhost:3000 with TypeScript strict
- FOUND-05 SATISFIED: src/app, src/components, src/engine, src/data, src/styles all exist
- FOUND-07 SATISFIED: git initialized, .gitignore covers Next.js artifacts
- FOUND-08 SATISFIED (dry run): `npm run build` succeeds before Tailwind/Vitest layered on
- React Strict Mode active in dev (per next.config.ts)
- Plan 02 (Style + Font) and Plan 03 (Vitest) can begin in Wave 1 in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-engine/01-01-SUMMARY.md` documenting:
- Final versions of next, react, typescript, tailwindcss installed (`npm list --depth=0`)
- Whether Tailwind v3 (default from create-next-app) or v4 was installed (Plan 02 may upgrade)
- Any tsconfig/strict-flag friction encountered in default scaffold files
- Confirmation that `.planning/` and `CLAUDE.md` were preserved by create-next-app
</output>
