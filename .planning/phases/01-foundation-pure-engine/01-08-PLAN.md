---
phase: 01-foundation-pure-engine
plan: 08
type: execute
wave: 6
depends_on: ["01-07"]
files_modified:
  - .planning/phases/01-foundation-pure-engine/01-VALIDATION.md
autonomous: false
requirements: [FOUND-08]
must_haves:
  truths:
    - "npm run build exits 0 with zero TypeScript errors and zero ESLint warnings"
    - "Full Vitest suite (30 tests across damage, turnQueue, reducer, enemyAI) is green"
    - "Coverage on src/engine/*.ts (excluding types.ts and test files) is >= 80% lines/branches/functions/statements"
    - "Every Phase 1 requirement ID in 01-VALIDATION.md task map is verified ✅ or has documented red"
    - "First Load JS for / route is reasonable for a Phase 1 skeleton (< 200kB target per VALIDATION.md manual checks)"
    - "No regressions: dev server still boots, BattleScene still transitions phases without double-fire"
  artifacts:
    - path: ".planning/phases/01-foundation-pure-engine/01-VALIDATION.md"
      provides: "Updated task map with status column reflecting actual verify results"
      contains: "✅"
  key_links:
    - from: "VALIDATION.md per-task verify map"
      to: "Plan 01-07 acceptance criteria"
      via: "Each task ID (1-XX-YY) traces to a Plan file's <verify> + <acceptance_criteria>"
      pattern: "1-0[1-7]-0"
---

<objective>
The Phase 1 capstone: prove the entire foundation is production-build clean, the full test suite is green with adequate coverage, and every requirement ID in 01-VALIDATION.md's task map is verified. Update the validation artifact in place to flip status indicators from `⬜ pending` to `✅ green` (or document any reds with reproduction steps).

This plan does NOT add new functionality — it verifies what Plans 01-07 produced and locks in the "Phase 1 complete" gate. After this plan ships, Phase 2 (Encounter 1) can begin in confidence that the engine, types, reducer, and Strict Mode safety net are all working.

Purpose: Without an explicit verification gate, "Phase 1 complete" is hand-wavy. This plan converts the VALIDATION.md task map from intent to evidence. It also sets the build size baseline and confirms no regressions snuck in across the parallel waves.

Output: Updated 01-VALIDATION.md with green checkmarks per task ID, a Phase 1 completion summary, and a Wave 0 → Wave 6 retrospective ready for Phase 2 to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-pure-engine/01-VALIDATION.md
@.planning/phases/01-foundation-pure-engine/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-pure-engine/01-01-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-02-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-03-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-04-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-05-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-06-SUMMARY.md
@.planning/phases/01-foundation-pure-engine/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full validation suite (build, tests, coverage, lint, type-check)</name>
  <files>(read-only — no files modified)</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-VALIDATION.md (entire task map and sampling rate sections)
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-RESEARCH.md (section 17 — build & verification matrix)
  </read_first>
  <action>
    Run each validation command from VALIDATION.md sampling map and RESEARCH §17. Capture exit codes and key output for each. Do NOT modify any source files in this task — purely verification.

    Use absolute paths. Run from `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG`.

    1. TypeScript clean:
       ```bash
       cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
       npx tsc --noEmit
       echo "tsc exit: $?"
       ```
       Expected: exit 0, zero errors output.

    2. ESLint clean:
       ```bash
       npm run lint
       echo "lint exit: $?"
       ```
       Expected: exit 0, zero errors, zero warnings.

    3. Full Vitest suite:
       ```bash
       npm run test -- --run 2>&1 | tee /tmp/vitest-final.log
       echo "test exit: $?"
       ```
       Expected: exit 0, "30 passed" in output (11 damage + 6 turnQueue + 9 reducer + 4 enemyAI).

    4. Coverage check:
       ```bash
       npm run test:coverage 2>&1 | tee /tmp/coverage-final.log
       echo "coverage exit: $?"
       ```
       Expected: exit 0. Coverage thresholds (80% lines/branches/functions/statements per vitest.config.ts) all met for `src/engine/*.ts` (excluding types.ts and *.test.ts per config exclude).

       INSPECT the coverage table — record the actual percentages for damage.ts, turnQueue.ts, reducer.ts, enemyAI.ts, gameStateRef.ts. If gameStateRef.ts shows < 80% — that's expected because it has no test in Phase 1; record as a known gap to be closed in Phase 2 with jsdom + RTL.

       NOTE: If gameStateRef.ts pulls coverage below the 80% threshold and fails the suite, you have two options:
       - (A) Add `'src/engine/gameStateRef.ts'` to `coverage.exclude` in `vitest.config.ts` — defer hook coverage to Phase 2 (recommended)
       - (B) Skip — accept the threshold failure and document it as a Phase 2 entry-criterion gap

       Choose option A if needed; this is a one-line change to `vitest.config.ts` (acceptable scope-creep within this validation task to keep CI green).

    5. Production build:
       ```bash
       npm run build 2>&1 | tee /tmp/build-final.log
       echo "build exit: $?"
       ```
       Expected: exit 0, "Compiled successfully" in output, zero warnings about Strict Mode or missing keys.

       INSPECT the build output table for the `/` route — record `First Load JS` size. VALIDATION.md "Manual-Only Verifications" sets a soft threshold of < 200kB; for a Phase 1 skeleton with one client component + Tailwind + Press Start 2P font, expect ~100-180 kB. If it's significantly higher, investigate (often a dependency tree-shake issue).

    6. Compile a results table for Task 2 to consume. Format:

       ```
       | Check                    | Exit | Result                                          |
       |--------------------------|------|-------------------------------------------------|
       | npx tsc --noEmit         | 0    | clean                                           |
       | npm run lint             | 0    | clean                                           |
       | npm run test -- --run    | 0    | 30 passed                                       |
       | npm run test:coverage    | 0    | damage.ts 100%, turnQueue.ts 100%, reducer.ts 95%, enemyAI.ts 90% (gameStateRef.ts excluded) |
       | npm run build            | 0    | First Load JS / route: 145 kB                   |
       ```

       Replace placeholder numbers with actual measured values. If any check FAILS (non-zero exit), DO NOT proceed to Task 2 — instead, identify the failing check and stop (this plan should not paper over real issues).
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && npx tsc --noEmit && npm run lint && npm run test -- --run 2>&1 | grep -qE "30 passed" && npm run build 2>&1 | grep -qE "(Compiled successfully|✓ Compiled)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm run test -- --run` exits 0 with "30 passed" in output
    - `npm run test:coverage` exits 0 (after option A or B applied if needed)
    - `npm run build` exits 0 with "Compiled successfully"
    - Results table compiled in task notes with actual measured values
    - No source files modified by this task (vitest.config.ts is the only allowed exception per option A above)
  </acceptance_criteria>
  <done>All five validation commands exit 0; results table captured for Task 2 to update VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md task map with verified status indicators</name>
  <files>.planning/phases/01-foundation-pure-engine/01-VALIDATION.md</files>
  <read_first>
    - /Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-VALIDATION.md (current state — task map has all ⬜ pending)
    - Results table from Task 1
  </read_first>
  <action>
    Open `/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG/.planning/phases/01-foundation-pure-engine/01-VALIDATION.md`.

    Update the "Per-Task Verification Map" table (lines 47-66 in current file). For each row:
    - Change `❌ W0` in "File Exists" column to `✅` (all Wave 0 work is now done)
    - Change `⬜ pending` in "Status" column to `✅ green` for tasks where Task 1's verification confirmed the requirement satisfied
    - For any task that had issues during execution, mark `⚠️ <description>` and add a note in the "Validation Sign-Off" section

    Update the frontmatter:
    - `status: draft` → `status: complete`
    - `nyquist_compliant: false` → `nyquist_compliant: true` (Wave 0 test infra exists, sampling rate met)
    - `wave_0_complete: false` → `wave_0_complete: true`

    Update the "Validation Sign-Off" checklist at the bottom — check all boxes that are now true. The final approval line should change from "pending" to "approved (Phase 1 complete YYYY-MM-DD)".

    Add a new section AFTER "Validation Sign-Off" titled `## Phase 1 Completion Summary`. Include:

    ```markdown
    ## Phase 1 Completion Summary

    **Completion date:** <today's date in ISO format>

    ### Test Results
    - Total tests: 30 passing (11 damage + 6 turnQueue + 9 reducer + 4 enemyAI)
    - Coverage thresholds: met (>= 80% per vitest.config.ts)
    - Build: clean (`npm run build` exit 0)
    - Lint: clean (`npm run lint` exit 0)
    - Type check: clean (`npx tsc --noEmit` exit 0)

    ### Build Size Baseline
    - First Load JS for `/` route: <actual kB from Task 1 measurement>
    - Phase 2 should keep this number below 250 kB even after adding character/enemy components

    ### Pitfall Guardrails Verified
    All 5 critical pitfalls neutralized as repeatable patterns:
    1. Strict Mode double-fire: animation-gate useEffect has clearTimeout cleanup; one-shot INIT uses useRef flag (not useState). Verified in browser by Plan 07 checkpoint.
    2. Stale closures: useGameStateRef hook + stateRef.current pattern in BattleScene setTimeout. Encoded in src/engine/gameStateRef.ts.
    3. Shallow mutations: reducer uses spread for ALL updates; mutation regression tests in damage.test.ts, turnQueue.test.ts, reducer.test.ts, enemyAI.test.ts.
    4. Turn race conditions: phase guard at top of every PLAYER_ACTION case in reducer; UI button disabled when phase != PLAYER_INPUT; verified by 3 reducer tests asserting reference identity.
    5. SSR hydration: 'use client' on BattleScene; no Math.random in render path (inverse grep verifies).

    ### Coverage Gaps (Documented)
    - `src/engine/gameStateRef.ts` excluded from Phase 1 coverage (no DOM test runner installed yet). Phase 2 entry criterion: add jsdom + @testing-library/react and write hook tests.
    - `src/data/characters.ts` and `src/data/enemies.ts` are constants with no logic — no test coverage needed.
    - Component layer (`src/components/BattleScene.tsx`, `src/components/SpriteFallback.tsx`) has no automated test in Phase 1 — Plan 07 checkpoint provides manual verification. Phase 2 will add component tests.

    ### Locked Decisions Honored
    1. ✅ useReducer at BattleScene root, NOT Zustand (verified by inverse grep `! grep -q zustand package.json`)
    2. ✅ ENGINE-04 phase enum is the literal string union per spec
    3. ✅ Tailwind v4 attempted with v3 fallback path documented (see Plan 02 SUMMARY)
    4. ✅ next/font/google with display: 'swap' per FOUND-04 (overrides PITFALLS preference for 'block')
    5. ✅ ASSETS-07 = CSS sprite fallback with clip-path silhouettes
    6. ✅ Phase 1 src/data/ scope minimal: DEADZONE + CASTING_PROBE_MK1 only

    ### Ready for Phase 2
    - Engine layer complete and tested
    - BattleScene shell working with Strict Mode safe patterns
    - 5 pitfall guardrails encoded as code patterns subsequent phases can lift verbatim
    - Build pipeline clean
    - Phase 2 (Encounter 1 — DEADZONE Solo) can begin: implement ENGINE-07..10 actions, SKILL-01 + SKILL-04, AI-02 (ALWAYS_ATTACK fully), full UI components per ENC-01
    ```

    Replace `<today's date in ISO format>` and `<actual kB from Task 1 measurement>` with the actual values from Task 1's results table.

    Save the file.
  </action>
  <verify>
    <automated>cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG" && grep -q "status: complete" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md && grep -q "nyquist_compliant: true" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md && grep -q "Phase 1 Completion Summary" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md && grep -q "30 passing" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md && ! grep -q "⬜ pending" .planning/phases/01-foundation-pure-engine/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `01-VALIDATION.md` frontmatter updated: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
    - All `⬜ pending` indicators in task map replaced with `✅ green` (or `⚠️` with documented red)
    - All `❌ W0` indicators in "File Exists" column replaced with `✅`
    - "Validation Sign-Off" checklist boxes checked
    - New section "Phase 1 Completion Summary" added with measured values from Task 1
    - Build size baseline recorded
    - All 6 locked decisions explicitly checked off
    - "Ready for Phase 2" section lists what Phase 2 should target
  </acceptance_criteria>
  <done>VALIDATION.md is the single source of truth for "Phase 1 complete" — all task IDs verified, completion summary added, ready for Phase 2 hand-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Final Phase 1 sign-off</name>
  <action>
    This is a human checkpoint task. The actionable work is described in <how-to-verify> below — the human user performs those steps directly. Claude pauses execution at this checkpoint until the user types the resume signal. No automated work to perform.
  </action>
  <what-built>Phase 1 (Foundation & Pure Engine) is complete. Tasks 1-2 of this plan verified every commit from Plans 01-07 against the VALIDATION.md task map and produced the completion summary. Before declaring Phase 1 done, the user should perform a final smoke pass.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/01-foundation-pure-engine/01-VALIDATION.md` — confirm:
       - Frontmatter shows `status: complete`, `wave_0_complete: true`
       - Per-task verification map has no `⬜ pending` rows
       - "Phase 1 Completion Summary" section exists with measured values

    2. Run a final smoke loop:
       ```bash
       cd "/Volumes/King Metror/Worldbuilding/[In]terfaces/Interfaces JRPG"
       npm run test -- --run     # 30 tests passing
       npm run build              # exits 0
       npm run dev                # boots, click Synthetic Action button once, watch phase transition
       ```

    3. Inspect the file tree — confirm Phase 1 deliverables present:
       - `src/app/{layout,page,globals.css}.tsx`
       - `src/components/{BattleScene,SpriteFallback}.tsx`
       - `src/engine/{types,damage,damage.test,turnQueue,turnQueue.test,reducer,reducer.test,enemyAI,enemyAI.test,gameStateRef}.ts`
       - `src/data/{characters,enemies}.ts`
       - `src/styles/sprite-fallback.module.css`
       - `vitest.config.ts`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`
       - All 8 plan SUMMARY files in `.planning/phases/01-foundation-pure-engine/`

    4. Read `01-VALIDATION.md` "Phase 1 Completion Summary" section — confirm the build size baseline number is reasonable (< 200 kB ideally for the `/` route).

    5. Confirm no Strict Mode warnings appear in the dev server's browser console during BattleScene mount and one click cycle.

    Report:
    - "PHASE 1 APPROVED" if everything checks out — Phase 2 planning can begin
    - "PHASE 1 NOT READY: <reasons>" otherwise; provide specific issues
  </how-to-verify>
  <resume-signal>Type "PHASE 1 APPROVED" to close out the foundation phase. Type "PHASE 1 NOT READY: <reasons>" to flag follow-up work needed before Phase 2 can begin.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → production build | Validation step ensures TypeScript strict + ESLint + Vitest catch issues before phase declared complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | Phase declared "complete" with hidden regressions across parallel waves | mitigate | This entire plan exists to verify; full test suite run + build run + manual checkpoint catches regressions |
| T-08-02 | I (Information Disclosure) | Build artifacts (`.next/`, `coverage/`) committed to git | accept | `.gitignore` from Plan 01 covers both directories |
| T-08-03 | D (Denial of Service) | gameStateRef.ts coverage gap pulls coverage threshold below 80% causing CI failure | mitigate | Documented option A in Task 1 — exclude gameStateRef.ts from coverage in vitest.config.ts; deferred to Phase 2 jsdom test |
</threat_model>

<verification>
- All 5 commands from Task 1 exit 0
- VALIDATION.md frontmatter shows `status: complete`, `wave_0_complete: true`, `nyquist_compliant: true`
- VALIDATION.md per-task map has no `⬜ pending` rows remaining
- VALIDATION.md "Phase 1 Completion Summary" section exists with measured values
- Final dev-server smoke (Task 3) confirms BattleScene transitions cleanly
- All Phase 1 locked decisions documented as honored
</verification>

<success_criteria>
- FOUND-08 SATISFIED (final verification): `npm run build` clean across all source files
- ROADMAP Phase 1 success criteria 1-5 all verifiable with green status in VALIDATION.md
- All 22 Phase 1 requirement IDs (FOUND-01..08, ENGINE-01..06, AI-01, AI-05, QA-01..05, ASSETS-07) confirmed satisfied
- Phase 2 (Encounter 1 — DEADZONE Solo) is unblocked
- VALIDATION.md is the single source of truth for "Phase 1 complete" — fully populated with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-engine/01-08-SUMMARY.md` documenting:
- Final test count (30) and per-file coverage percentages
- Final build size for `/` route (First Load JS in kB)
- Confirmation that all 5 pitfall guardrails are encoded and verified (with file:line refs)
- Confirmation that all 6 locked planning decisions are honored
- Phase 2 readiness: list what Phase 2 should target (ENGINE-07..10 actions, SKILL-01, SKILL-04, AI-02 full impl, ENC-01 UI components)
- Any TODOs handed off to Phase 2 (e.g., gameStateRef coverage gap, jsdom + RTL install, Plan 02 fallback documentation review)
</output>
