---
phase: 02-encounter-1-deadzone-solo
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/engine/gameStateRef.test.ts
  - src/engine/reducer.test.ts
  - src/engine/enemyAI.test.ts
  - src/components/ActionMenu.test.tsx
  - src/components/BattleLog.test.tsx
  - src/components/GameOverScreen.test.tsx
  - src/components/BattleScene.test.tsx
autonomous: true
requirements:
  - ENGINE-07
  - ENGINE-08
  - ENGINE-09
  - ENGINE-10
  - SKILL-01
  - SKILL-04
  - AI-02
  - UI-01
  - UI-02
  - UI-03
  - UI-07
  - END-02
  - END-03
  - END-04

must_haves:
  truths:
    - "npm run test -- --run exits 0 with all engine tests still green (30 tests minimum)"
    - "vitest.config.ts routes component test files to jsdom environment via environmentMatchGlobs"
    - "vitest.config.ts include glob covers src/**/*.test.{ts,tsx} so component tests are discovered"
    - "Test scaffold files exist in both src/engine/ and src/components/ with pending/skipped tests for all Phase 2 behaviors"
    - "gameStateRef.test.ts covers: ref reflects latest state, ref updates on re-render, ref is stable across renders"
  artifacts:
    - path: "vitest.config.ts"
      provides: "dual-env config — node for engine, jsdom for components"
      contains: "environmentMatchGlobs"
    - path: "src/engine/gameStateRef.test.ts"
      provides: "0% coverage gap closure for gameStateRef.ts"
      exports: []
    - path: "src/components/ActionMenu.test.tsx"
      provides: "scaffold for ActionMenu component tests"
    - path: "src/components/BattleLog.test.tsx"
      provides: "scaffold for BattleLog component tests"
    - path: "src/components/GameOverScreen.test.tsx"
      provides: "scaffold for GameOverScreen + retry button"
    - path: "src/components/BattleScene.test.tsx"
      provides: "scaffold for BattleScene integration smoke tests"
  key_links:
    - from: "vitest.config.ts"
      to: "src/components/**/*.test.tsx"
      via: "environmentMatchGlobs jsdom routing"
      pattern: "environmentMatchGlobs"
    - from: "package.json"
      to: "@testing-library/react"
      via: "npm install -D"
      pattern: "@testing-library/react"
---

<objective>
Install jsdom + @testing-library/react + @testing-library/user-event and update vitest.config.ts to support dual environments (node for engine, jsdom for component tests). Create test scaffold files for every Phase 2 component and close the 0% coverage gap on gameStateRef.ts deferred from Phase 1.

Purpose: Phase 2 produces five UI components and wires the full battle loop. These components require jsdom for testing — without this Wave 0 setup, no component test can run. The gameStateRef.ts hook has 0% coverage since Phase 1 deferred it; Phase 2 must close that gap.

Output: npm install adds 3 devDependencies; vitest.config.ts gains environmentMatchGlobs + expanded include glob; 5 test scaffold files created in src/components/; gameStateRef.test.ts added to src/engine/; npm run test exits 0 with all prior 30 tests plus new gameStateRef tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-encounter-1-deadzone-solo/02-RESEARCH.md
@.planning/phases/01-foundation-pure-engine/01-05-SUMMARY.md

<interfaces>
<!-- From src/engine/gameStateRef.ts (exists, 0% test coverage) -->
```typescript
export function useGameStateRef(state: BattleState): React.RefObject<BattleState>
// useRef<BattleState>(state) + useEffect(() => { ref.current = state }, [state])
// Tests need jsdom + renderHook from @testing-library/react
```

<!-- From vitest.config.ts (current state — must change) -->
```typescript
test: {
  environment: 'node',           // ← keep as default
  globals: true,
  include: ['src/engine/**/*.test.ts'],  // ← MUST expand to src/**/*.test.{ts,tsx}
  coverage: {
    include: ['src/engine/**/*.ts'],
    exclude: ['src/engine/**/*.test.ts', 'src/engine/types.ts', 'src/engine/gameStateRef.ts'],
    // ← remove gameStateRef.ts from exclude once test exists
  },
}
// ADD: environmentMatchGlobs for jsdom routing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install testing dependencies and update vitest.config.ts</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current config — understand what changes)
    - package.json (verify @testing-library not already present)
  </read_first>
  <action>
Run the install command exactly:
```
npm install -D @testing-library/react@^16 @testing-library/user-event@^14 jsdom@^25
```

Then rewrite vitest.config.ts to add dual-environment support. The final config must:

1. Keep `environment: 'node'` as the default (engine tests stay fast, no DOM overhead).
2. Add `environmentMatchGlobs` array to route component test files to jsdom:
   ```typescript
   environmentMatchGlobs: [
     ['src/components/**/*.test.tsx', 'jsdom'],
     ['src/engine/gameStateRef.test.ts', 'jsdom'],
   ],
   ```
3. Expand `include` from `'src/engine/**/*.test.ts'` to `['src/**/*.test.{ts,tsx}']` so component test files are discovered by the test runner.
4. In the coverage config: expand `include` from `'src/engine/**/*.ts'` to `['src/engine/**/*.ts', 'src/components/**/*.tsx']` (components will be covered once tests are written).
5. Remove `'src/engine/gameStateRef.ts'` from coverage `exclude` — we are closing that gap this wave.
6. Keep all other coverage settings (thresholds, reporter, provider) unchanged.

Do NOT add setupFiles or globalSetup — RTL works without them in Vitest 2 + jsdom.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies includes `@testing-library/react`, `@testing-library/user-event`, and `jsdom` (check: `grep "@testing-library" package.json`)
    - `vitest.config.ts` contains `environmentMatchGlobs` (check: `grep "environmentMatchGlobs" vitest.config.ts`)
    - `vitest.config.ts` contains `src/**/*.test.{ts,tsx}` in include (check: `grep "test.{ts,tsx}" vitest.config.ts`)
    - `vitest.config.ts` does NOT contain `'src/engine/gameStateRef.ts'` in the exclude list
    - `npm run test -- --run` exits 0 with at least 30 tests passing (prior suite must not regress)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>vitest.config.ts updated, 3 devDependencies installed, prior 30 tests still green, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds — gameStateRef + component stubs</name>
  <files>
    src/engine/gameStateRef.test.ts,
    src/components/ActionMenu.test.tsx,
    src/components/BattleLog.test.tsx,
    src/components/GameOverScreen.test.tsx,
    src/components/BattleScene.test.tsx
  </files>
  <read_first>
    - src/engine/gameStateRef.ts (the hook being tested — read its implementation)
    - src/engine/types.ts (BattleState type for test fixtures)
    - vitest.config.ts (verify it was updated in Task 1)
    - .planning/phases/02-encounter-1-deadzone-solo/02-RESEARCH.md §Validation Architecture (test map)
  </read_first>
  <behavior>
    gameStateRef.test.ts (jsdom env, uses renderHook):
    - Test 1: "ref reflects initial state" — renderHook with initial BattleState, expect ref.current.phase === 'INIT'
    - Test 2: "ref updates when state changes" — rerender with new state, expect ref.current matches new state
    - Test 3: "ref object is stable (same reference) across renders" — capture ref before rerender, expect same === identity after rerender

    ActionMenu.test.tsx (jsdom, skeleton only — tests are todo/skipped for now):
    - describe('ActionMenu') with it.todo for: renders 4 buttons, HABILIDADE disabled when EN lt 8, ITEM disabled when nanoMed=0, keyboard shortcut 1 fires ATACAR, keyboard shortcut 2 fires HABILIDADE, keyboard shortcut 3 fires DEFENDER, keyboard shortcut 4 fires ITEM

    BattleLog.test.tsx (jsdom, skeleton):
    - describe('BattleLog') with it.todo for: renders all log entries, renders empty state, scrolls to bottom on update

    GameOverScreen.test.tsx (jsdom, skeleton):
    - describe('GameOverScreen') with it.todo for: renders GAME OVER text, renders TENTAR NOVAMENTE button, clicking retry calls onRetry prop

    BattleScene.test.tsx (jsdom, skeleton):
    - describe('BattleScene') with it.todo for: renders battle layout, shows DEADZONE HP, shows enemy HP, renders GAME OVER when phase is GAME_OVER, renders victory state when phase is VICTORY
  </behavior>
  <action>
Create each test file. The gameStateRef.test.ts must have REAL passing tests (not todos). The four component test files contain only it.todo stubs — they define the test contract but do not yet implement it (implementation comes in Waves 1-3 when the components exist).

For gameStateRef.test.ts, import `renderHook, act` from `@testing-library/react`. Build a minimal BattleState fixture (use the shape from types.ts — phase: 'INIT', party: [], enemies: [], etc.). The hook signature is `useGameStateRef(state: BattleState)` returning `React.RefObject<BattleState>`.

For component scaffolds: import React at top (required for JSX in test files), add the describe block and it.todo items. Do not import the components yet — they don't exist. The file just needs to compile (it.todo takes only a string, no callback).

Verify: `npm run test -- --run` must exit 0. The gameStateRef tests must PASS (green). The it.todo items are reported as skipped, not failed.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | grep -E "pass|fail|todo|skip"</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/gameStateRef.test.ts` exists with 3 passing tests (not todos)
    - `src/components/ActionMenu.test.tsx` exists with describe block + at least 4 it.todo items
    - `src/components/BattleLog.test.tsx` exists with describe block + at least 2 it.todo items
    - `src/components/GameOverScreen.test.tsx` exists with describe block + at least 2 it.todo items
    - `src/components/BattleScene.test.tsx` exists with describe block + at least 3 it.todo items
    - `npm run test -- --run` exits 0 (all real tests pass, todos are skipped not failed)
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run 2>&1 | grep "gameStateRef"` shows test file ran and passed
  </acceptance_criteria>
  <done>5 test files created, 3 gameStateRef tests green, 0% coverage gap on gameStateRef.ts closed, prior 30 tests unaffected, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-dependency install | npm pulls packages from registry; lock file must be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | package-lock.json | mitigate | Commit lock file after install; never use --ignore-scripts in CI |
| T-02-01-02 | Information | gameStateRef.test.ts | accept | Test file contains no secrets or PII; state fixtures are synthetic game data |
</threat_model>

<verification>
Run after completing both tasks:
```bash
npm run test -- --run
npx tsc --noEmit
grep "environmentMatchGlobs" vitest.config.ts
grep "@testing-library/react" package.json
```

All must exit 0. The test run must report 33+ tests (30 engine + 3 gameStateRef).
</verification>

<success_criteria>
- npm install succeeds and package-lock.json updated
- vitest.config.ts has environmentMatchGlobs routing component tests to jsdom
- vitest.config.ts include glob covers `src/**/*.test.{ts,tsx}`
- gameStateRef.test.ts: 3 tests, all passing, runs in jsdom env
- 4 component scaffold files exist with it.todo stubs that compile and are reported as skipped
- All 30 prior engine tests still green
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-encounter-1-deadzone-solo/02-01-SUMMARY.md`
</output>
