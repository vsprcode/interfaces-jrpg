/**
 * Test scaffold for BattleLog component.
 * Runs in jsdom environment (see vitest.config.ts environmentMatchGlobs).
 *
 * Tests are it.todo — implementation comes in Wave 1 when the component exists.
 * This file defines the test contract for BattleLog.
 */
import React from 'react';

describe('BattleLog', () => {
  it.todo('renders all log entries passed as props');
  it.todo('renders empty state when log array is empty');
  it.todo('scrolls to bottom when log entries update');
});
