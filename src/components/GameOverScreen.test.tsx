/**
 * Test scaffold for GameOverScreen component.
 * Runs in jsdom environment (see vitest.config.ts environmentMatchGlobs).
 *
 * Tests are it.todo — implementation comes in Wave 1 when the component exists.
 * This file defines the test contract for GameOverScreen.
 */
import React from 'react';

describe('GameOverScreen', () => {
  it.todo('renders GAME OVER text');
  it.todo('renders TENTAR NOVAMENTE retry button');
  it.todo('clicking retry button calls onRetry prop');
});
